#include <iostream>
using namespace std;
int main() {
	int i = 0, p = 12;
	char space = 32, ch;
	char dst[10000] = { '\0' };
	// 这是注释啊
	cout << "请输入代码(以'#'结束):" << endl;
	const char* fpath = "input.txt";
	fWrite(fp, fpath, ch, src, dst);
	/*
	    这也是注释啊
	*/
	Preprocessing(src, dst);
	return 0;
}